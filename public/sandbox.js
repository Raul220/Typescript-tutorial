"use strict";
// let greet : Function;
// Example 1
let greet;
greet = (name, greet) => console.log(`${name} says ${greet}`);
// Example 2
let calc;
calc = (numOne, numTwo, action) => {
    return action === 'add' ? numOne + numOne : numOne - numTwo;
};
// Example 3
let log_details;
log_details = (person) => console.log(`${person.name} is ${person.age} years old`);
