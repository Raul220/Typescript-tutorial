"use strict";
const log_details = (uid, item) => console.log(`${item} has a uid of ${uid}`);
const greet = (user) => console.log(`${user.name} has a uid of ${user.uid}`);
const log_details_again = (uid, item) => console.log(`${item} has a uid of ${uid}`);
const greet_again = (user) => console.log(`${user.name} has a uid of ${user.uid}`);
const greet_again_1 = (user) => console.log(`${user.name} has a uid of ${user.uid}`);
